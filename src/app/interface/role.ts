export interface Role {
  Role(): void;
}

export interface Data {
  empID: number;
  name: string;
  age: number;
  address: string;
}

// interface  is basically like a contract which mean evry class have to use it with there needs
